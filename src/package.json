{
  "scripts": {
    "parse": "ts-node --transpile-only parser/dev",
    "compile": "ts-node --transpile-only compiler/dev",
    "balls": "npm run parse && npm run compile",
    "update": "cd .. && cd output && git pull"
  },
  "devDependencies": {
    "@types/he": "^1.1.2",
    "@types/marked": "^4.0.2",
    "@types/node": "^17.0.18",
    "@types/pluralize": "^0.0.29"
  },
  "dependencies": {
    "he": "^1.2.0",
    "lodash": "^4.17.21",
    "marked": "^4.0.12",
    "pluralize": "^8.0.0",
    "typescript": "^4.5.5"
  }
}