{
  "scripts": {
    "parse": "ts-node --transpile-only parser/dev",
    "compile": "ts-node --transpile-only compiler/dev",
    "update": "cd .. && cd discord-api-docs && git pull",
    "all": "npm run update && npm run parse && npm run compile"
  },
  "devDependencies": {
    "@types/he": "^1.1.2",
    "@types/marked": "^4.0.2",
    "@types/node": "^17.0.18",
    "@types/pluralize": "^0.0.29"
  },
  "dependencies": {
    "@discordjs/rest": "^1.1.0",
    "@discordjs/ws": "^0.3.0",
    "chalk": "^4.1.2",
    "he": "^1.2.0",
    "marked": "^4.0.12",
    "pluralize": "^8.0.0",
    "typescript": "^4.6.3"
  }
}
